language: generic
sudo: require
services:
    - docker
before_install:
    - docker build -t marinalejandro/docker-react -f Dockerfile.dev .
script:
    - docker run -e CI=true marinalejandro/docker-react npm run test
